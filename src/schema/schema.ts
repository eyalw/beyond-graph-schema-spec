import { GraphSchemaMeta } from "./schemaTypes";
import { entityTypes } from "./entities";
import { relationshipTypes } from "./relationships";

export const schema: GraphSchemaMeta = {
  entityTypes,
  relationshipTypes
};
